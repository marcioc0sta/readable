import styled from 'styled-components';

const Container = styled.div`
  display: flex;
  padding: 24px 24px 0;
`;

const styles = () => ({
  chip: {
    marginLeft: 5,
    marginRight: 5,
  }
});

export {
  Container,
  styles,
}
